using UnityEngine;

///Vars.cs: contains global static variables.
public class Vars : MonoBehaviour {

    public enum Rotation
    {
        forward, 
        right,
        back, 
        left
    }
}